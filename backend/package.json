{
  "name": "@locket/backend",
  "scripts": {
    "start": "clojure -Arun",
    "test": "clojure -Atest",
    "test-watch": "clojure -Atest --watch",
    "build": "clojure -Auberjar",
    "clean": "rimraf target"
  },
  "devDependencies": {
    "@locket/db": "0.1.2",
    "rimraf": "~3.0.2"
  }
}
